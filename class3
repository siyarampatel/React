onChange() function is used to tackle input feild in react
---taking input and showing its output on screen---
import React, { use, useState } from 'react'

const App = () => {
  let[input,SetInput]=useState("")
  function fun(e){
    SetInput(e.target.value)
  }
  return (
    <div>
      <h2>{input}</h2>
      <input type="text" onChange={fun}/>
    </div>
  )
}

export default App

----------it should show the input after clicking button (not instant)--------
import React, { useState } from 'react'

const App = () => {
  let [input,SetInput] = useState("");
  let [data,SetData] = useState("");

  function set(e){
    SetInput(e.target.value);
  }
  function done(){
    SetData(input);
  }
  return (
    <div>
      <h2>{data}</h2>
      <input type="text" onChange={set}/>
      <button onClick={done}>click</button>
    </div>
  )
}

export default App

------on click input field must be clear-------
import React, { useState } from 'react'

const App = () => {
  let [input,SetInput] = useState("");
  let [data,SetData] = useState("");

  function set(e){
    SetInput(e.target.value);
  }
  function done(){
    SetData(input);
    SetInput("")
  }
  return (
    <div>
      <h2>{data}</h2>
      <input type='text' name='input' value={input} onChange={set}/>
      <button onClick={done}>click</button>
    </div>
  )
}

export default App

---------------Form-----------
import React from 'react'
import Form  from './Form'

const App = () => {
  return (
    <div>
      <Form/>
    </div>
  )
}

export default App


import React, { useState } from 'react'
import './App.css'

const Form = () => {
    let[input,SetInput] = useState({
        name:"",
        email:"",
        password:""
    })

    function fun1(e){
        // console.log(e.target)
        let{name,value}=e.target 
        SetInput({...input,[name]:value})
        console.log(input)
    }
  return (
    <div>
        <form>
            <input type="text"  onChange={fun1} placeholder='Enter your name' name='name' value={input.name}  />
            <br></br>
            <br></br>

            <input type="text"  onChange={fun1} placeholder='Enter your email' name='email' value={input.email}  />
            <br></br>
            <br></br>

            <input type="password"  onChange={fun1} placeholder='Enter your password' name='password' value={input.password}  />
            <br></br>
            <br></br>

            <button>Submit</button>

        </form>
    </div>
  )
}

export default Form


---we wanteed to remember which input field has what values -------
